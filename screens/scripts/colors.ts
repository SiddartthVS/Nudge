/**
 * The app's color palette. Every screen and component imports its colors
 * from here rather than writing hex codes inline, so the whole app can be
 * re-themed by changing values in one place.
 */
export const Colors = {
  /** Primary accent - "good"/positive bars, granted permissions, active pills. */
  green: '#20C997',
  /** Secondary accent - alternating bars, ungranted permissions, warnings. */
  orange: '#FF6B6B',
  /** App-wide background, behind every screen. */
  background: '#121212',
  /** Primary text/icon color on dark backgrounds. */
  text: '#FFFFFF',
  /** Card and control background - one shade lighter than `background`. */
  grey: '#383838',
};
